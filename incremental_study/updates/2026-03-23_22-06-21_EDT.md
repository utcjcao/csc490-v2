# Incremental Study Update

Timestamp: `2026-03-23 22:06:21 EDT`

## Summary

Added a dedicated runtime dependency file for the standalone study and expanded
the README with explicit installation and run instructions.

## New File

- [requirements.txt](/Users/chris/Developer/school/csc490-v2/incremental_study/requirements.txt)

This file now captures the core Python runtime dependencies for the
`incremental_study` harness.

## What Changed in the README

Updated [README.md](/Users/chris/Developer/school/csc490-v2/incremental_study/README.md) to include:
- environment creation with `venv`
- a single install command using `requirements.txt`
- optional ONNX dependency installation
- notes about installing the correct CUDA-enabled PyTorch build on remote GPU machines
- instructions for downloading model weights
- example run commands
- quick-start setup instructions

## Current Recommended Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r incremental_study/requirements.txt
```

Optional ONNX support:

```bash
pip install "onnx>=1.8" "onnx2pytorch>=0.4.1"
```

## Why This Helps

This makes the standalone study easier to reproduce because setup is now:
- explicit
- local to the `incremental_study` folder
- independent of IVAN's old dependency stack
- independent of the abcrown setup instructions

## Scope

This update only improved setup documentation and dependency packaging.

It did not change:
- verification semantics
- perturbation logic
- logged artifacts
- the standalone paired-analysis workflow
