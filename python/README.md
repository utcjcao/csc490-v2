# Python Worker Packages

The Python side of the repository contains verifier-facing code:

- `adapter-common`: shared manifest parsing and adapter utilities
- `alpha-beta-crown-adapter`: the first verifier adapter package

These packages are intentionally light in the scaffold. The real verifier integration work starts in Phase 1 task 8.

