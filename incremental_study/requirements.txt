# Core runtime dependencies for the standalone incremental_study harness.
#
# If you are on a remote GPU machine, you may want to install the correct
# CUDA-enabled torch/torchvision wheels first from pytorch.org, then run:
#   pip install -r incremental_study/requirements.txt
#
# ONNX support is optional and can be installed separately with:
#   pip install "onnx>=1.8" "onnx2pytorch>=0.4.1"

torch>=2.0,<2.9
torchvision>=0.12,<0.24
numpy>=1.20
packaging>=20.0
appdirs>=1.4
pyyaml>=5.0
ninja>=1.10
tqdm>=4.64
graphviz>=0.20.3
