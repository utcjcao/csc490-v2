openapi: 3.1.0
info:
  title: Incremental Verification Control Plane API
  version: 0.1.0
  description: Phase 1 scaffold API contract placeholder.
paths:
  /healthz:
    get:
      summary: Health probe
      operationId: getHealth
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    enum: [ok]
  /v1/verification-runs:
    post:
      summary: Submit a verification run
      operationId: createVerificationRun
      responses:
        "501":
          description: Not implemented in the Phase 1 scaffold

