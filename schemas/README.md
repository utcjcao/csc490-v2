# Schemas

Generated and hand-authored contract snapshots live here.

- `api/v1/`: OpenAPI documents for REST endpoints
- `worker/v1/`: JSON Schemas for verifier worker manifests and results

The current files are intentionally minimal and should be kept in sync with `crates/contracts`.

