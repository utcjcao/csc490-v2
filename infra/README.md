# Infrastructure

- `docker/`: image build definitions
- `compose/`: local development stack
- `k8s/`: future scale-out manifests

The current files are placeholders that match the architecture docs and are intended to evolve during Phase 1 and Phase 2.

