# Tests

Top-level test buckets:

- `integration/`: Rust integration tests against real infrastructure
- `contract/`: Rust/Python contract and schema compatibility tests
- `e2e/`: end-to-end baseline and incremental flow tests

The scaffold keeps these mostly empty on purpose; real fixtures and tests land as core flows are implemented.

