# Integration Tests

Place broader integration tests here as the persistence and storage adapters expand.

Current coverage for the durable Phase 1 run flow lives closer to the app crates and uses SQLite-backed local persistence. Add PostgreSQL and MinIO integration suites here only when those adapters become active parts of the runtime.
