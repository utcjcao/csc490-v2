# Database

This directory holds relational schema migrations for the future PostgreSQL metadata store.

Current Phase 1 local/dev persistence uses SQLite and creates its narrow schema directly in the adapter. The placeholder migration here is retained for the later PostgreSQL migration target, not for the current canonical run flow.
