# adapter-common

Shared Python utilities for verifier adapters.

Current contents:

- dataclass-based worker contract definitions
- manifest load/write helpers
- placeholder storage helpers
- adapter error types

This package intentionally avoids verifier-specific logic.

